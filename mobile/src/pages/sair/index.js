import React from 'react';
import { View, Text } from 'react-native';
import Styles from './styles';

export default function remessa(){
	return (
		<View style={Styles.container}>
            <Text>Pagina Sair</Text>
		</View>
    );
}